import RadioAntD from 'antd/lib/radio';
import React from 'react';
import { connectField, filterDOMProps } from 'uniforms';
import wrapField from './wrapField';
const radioStyle = { display: 'block' };
function Radio(props) {
    var _a, _b;
    return wrapField(props, React.createElement(RadioAntD.Group, Object.assign({ disabled: props.disabled, name: props.name, onChange: event => {
            if (!props.readOnly) {
                props.onChange(event.target.value);
            }
        }, value: (_a = props.value) !== null && _a !== void 0 ? _a : '' }, filterDOMProps(props)), (_b = props.allowedValues) === null || _b === void 0 ? void 0 : _b.map(value => (React.createElement(RadioAntD, { key: value, style: radioStyle, value: value }, props.transform ? props.transform(value) : value)))));
}
export default connectField(Radio, { kind: 'leaf' });
